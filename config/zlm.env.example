# AI Monitor ZLMediaKit runtime config
# ZLMediaKit is treated as part of the base environment, not the app release.
# Point these paths to the already-installed MediaServer and its validated config.ini.

AI_MONITOR_ZLM_BIN=/home/hzhy/ZLMediaKit/release/linux/Debug/MediaServer
AI_MONITOR_ZLM_CONFIG_PATH=/home/hzhy/ZLMediaKit/release/linux/Debug/config.ini
